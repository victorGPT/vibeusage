## 1. Implementation
- [x] Add sign-up URL in `dashboard/src/App.jsx` and pass to landing page.
- [x] Add Login and Sign Up buttons to landing header using `MatrixButton`.
- [x] Register copy keys in `dashboard/src/content/copy.csv`.

## 2. Tests
- [x] Run `node scripts/validate-copy-registry.cjs`.

## 3. Verification
- [ ] Manual: `npm --prefix dashboard run dev` and verify header buttons + link targets.

## 4. Docs
- [ ] Update verification report with commands and results.
