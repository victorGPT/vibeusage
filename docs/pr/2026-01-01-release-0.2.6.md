# PR Template (Minimal)

## PR Goal (one sentence)
Record the 0.2.6 release notes and regression verification after publishing.

## Commit Narrative
- Commit 1: `release: 0.2.6`
- Commit 2: `docs: record 0.2.6 release verification`

## Regression Test Gate
### Most likely regression surface
- CLI init flow (consent prompt, local report, auth transition, success box).

### Verification method (choose at least one)
- [x] `npm test` => PASS
- [x] `node scripts/acceptance/npm-install-smoke.cjs` => PASS
- [x] Published package smoke: `VIBESCORE_RUN_NPX=1 node scripts/acceptance/npm-install-smoke.cjs` => PASS

### Uncovered scope
- Real-world init on a machine with Codex + Claude + Gemini all installed.

## Notes
- Release published as `@vibescore/tracker@0.2.6`.
- Git tag: `v0.2.6`.
