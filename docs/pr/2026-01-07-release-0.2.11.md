# PR Template (Minimal)

## PR Goal (one sentence)
Record the 0.2.11 release notes and regression verification after publishing.

## Commit Narrative
- Commit 1: `release: 0.2.11`
- Commit 2: `docs: record 0.2.11 release verification`

## Regression Test Gate
### Most likely regression surface
- Opencode usage parsing and release packaging smoke.

### Verification method (choose at least one)
- [x] `npm test` => PASS
- [x] `node --test test/rollout-parser.test.js` => PASS
- [x] Pre-publish smoke: `node scripts/acceptance/npm-install-smoke.cjs` => PASS
- [x] Published package smoke: `VIBESCORE_RUN_NPX=1 node scripts/acceptance/npm-install-smoke.cjs` => PASS

### Uncovered scope
- Live Opencode storage replay against production accounts.

## Notes
- Release published as `vibeusage@0.2.11`.
- Git tag: `v0.2.11`.
