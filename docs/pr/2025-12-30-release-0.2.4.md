# PR Template (Minimal)

## PR Goal (one sentence)
Record the 0.2.4 release notes and regression verification after publishing.

## Commit Narrative
- Commit 1: `release: 0.2.4`

## Regression Test Gate
### Most likely regression surface
- CLI init/uninstall paths when Codex is absent or configured via CODEX_HOME.

### Verification method (choose at least one)
- [x] `node --test test/init-uninstall.test.js` => PASS
- [x] `VIBESCORE_RUN_NPX=1 node scripts/acceptance/npm-install-smoke.cjs` => PASS

### Uncovered scope
- Real-world init on a machine without Codex installed.

## Notes
- Release published as `@vibescore/tracker@0.2.4`.
- Git tag: `v0.2.4`.
