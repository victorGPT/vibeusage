# PR Template (Minimal)

## PR Goal (one sentence)
Record the 0.2.1 release notes and regression verification after publishing.

## Commit Narrative
- Commit 1: `docs: record 0.2.1 release notes`

## Regression Test Gate
### Most likely regression surface
- CLI init sync drain + dashboard install copy flow + link code exchange.

### Verification method (choose at least one)
- [x] Existing automated tests did not fail (commands: `node --test test/init-uninstall.test.js test/init-spawn-error.test.js`, `node --test test/dashboard-link-code-install.test.js test/dashboard-link-code-expiry.test.js test/dashboard-render-order.test.js`, `node --test test/edge-functions.test.js test/link-code-exchange-payload.test.js` => PASS)
- [x] New minimal test added and executed (command: `node scripts/acceptance/link-code-exchange.cjs` => PASS)
- [ ] Manual regression path executed

### Uncovered scope
- Live dashboard UI smoke after production deploy.

## Notes
- Release published as `@vibescore/tracker@0.2.1`.
