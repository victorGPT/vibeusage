name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      functions: ${{ steps.changes.outputs.functions }}
      dashboard: ${{ steps.changes.outputs.dashboard }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Detect changed areas
        id: changes
        shell: bash
        run: |
          set -euo pipefail
          BEFORE="${{ github.event.before }}"
          AFTER="${{ github.sha }}"
          if [ -z "$BEFORE" ] || [ "$BEFORE" = "0000000000000000000000000000000000000000" ]; then
            BEFORE="${AFTER}^"
          fi
          git diff --name-only "$BEFORE" "$AFTER" > /tmp/changed.txt
          if grep -E -q '^(insforge-src/functions/|insforge-functions/)' /tmp/changed.txt; then
            echo "functions=true" >> "$GITHUB_OUTPUT"
          else
            echo "functions=false" >> "$GITHUB_OUTPUT"
          fi
          if grep -E -q '^dashboard/' /tmp/changed.txt; then
            echo "dashboard=true" >> "$GITHUB_OUTPUT"
          else
            echo "dashboard=false" >> "$GITHUB_OUTPUT"
          fi

  preflight:
    runs-on: ubuntu-latest
    needs: changes
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Verify model alias table exists
        env:
          INSFORGE_BASE_URL: ${{ secrets.INSFORGE_BASE_URL }}
          INSFORGE_ANON_KEY: ${{ secrets.INSFORGE_ANON_KEY }}
          INSFORGE_SERVICE_ROLE_KEY: ${{ secrets.INSFORGE_SERVICE_ROLE_KEY }}
          INSFORGE_API_KEY: ${{ secrets.INSFORGE_API_KEY }}
        run: node scripts/acceptance/model-identity-alias-table.cjs

  ci-gate:
    runs-on: ubuntu-latest
    needs: [changes, preflight]
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - name: Install root dependencies
        run: npm ci
      - name: Install dashboard dependencies
        run: npm --prefix dashboard ci
      - name: Run release CI gate
        run: npm run ci:local

  npm-publish:
    runs-on: ubuntu-latest
    needs: [changes, preflight, ci-gate, vercel-gate, mcp-confirmation, mcp-skip]
    if: >-
      always() &&
      github.ref == 'refs/heads/main' &&
      needs.preflight.result == 'success' &&
      needs.ci-gate.result == 'success' &&
      needs.vercel-gate.result == 'success' &&
      (
        (needs.changes.outputs.functions == 'true' && needs.mcp-confirmation.result == 'success') ||
        (needs.changes.outputs.functions == 'false' && needs.mcp-skip.result == 'success')
      )
    timeout-minutes: 10
    # Required for npm Trusted Publishing (OIDC).
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Read package metadata
        id: package
        run: |
          node -e "const pkg=require('./package.json'); console.log('name='+pkg.name); console.log('version='+pkg.version);" >> "$GITHUB_OUTPUT"
      - name: Check npm registry for version
        id: npm
        run: |
          set -euo pipefail
          NAME="${{ steps.package.outputs.name }}"
          VERSION="${{ steps.package.outputs.version }}"
          if npm view "$NAME@$VERSION" version >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Publish to npm
        if: steps.npm.outputs.exists != 'true'
        # Use npx to invoke the latest npm CLI directly (Trusted Publishing requires
        # npm >= 11.5.1; global install corrupts bundled node_modules on self-upgrade).
        run: npx --yes npm@latest publish
      - name: Skip publish (version exists)
        if: steps.npm.outputs.exists == 'true'
        run: echo "Version already published; skipping."

  vercel-gate:
    runs-on: ubuntu-latest
    needs: [changes, preflight, ci-gate]
    timeout-minutes: 25
    permissions:
      checks: read
      contents: read
    env:
      VERCEL_CHECK_NAME: ${{ vars.VERCEL_CHECK_NAME || 'Vercel' }}
      VERCEL_TIMEOUT_MINUTES: "20"
      VERCEL_POLL_SECONDS: "20"
    steps:
      - name: Skip Vercel gate (no dashboard changes)
        if: needs.changes.outputs.dashboard != 'true'
        run: echo "No dashboard changes detected; Vercel gate satisfied."
      - name: Wait for Vercel deployment check
        if: needs.changes.outputs.dashboard == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const targetName = (process.env.VERCEL_CHECK_NAME || 'Vercel').trim();
            const timeoutMinutes = Number(process.env.VERCEL_TIMEOUT_MINUTES || 20);
            const pollSeconds = Number(process.env.VERCEL_POLL_SECONDS || 20);
            const deadline = Date.now() + timeoutMinutes * 60 * 1000;
            const { owner, repo } = context.repo;
            const ref = context.sha;

            function matchContext(contextName) {
              if (!contextName) return false;
              // Default behavior: allow Vercel's project-scoped checks like "Vercel – <project>".
              if (targetName === 'Vercel') return contextName.startsWith('Vercel');
              if (targetName.endsWith('*')) return contextName.startsWith(targetName.slice(0, -1));
              return contextName === targetName;
            }

            function latestPerContext(statuses) {
              const byContext = new Map();
              for (const status of statuses) {
                if (!status?.context) continue;
                const existing = byContext.get(status.context);
                if (!existing) {
                  byContext.set(status.context, status);
                  continue;
                }
                const prevTime = new Date(existing.created_at || 0).getTime();
                const nextTime = new Date(status.created_at || 0).getTime();
                if (nextTime > prevTime) byContext.set(status.context, status);
              }
              return Array.from(byContext.values());
            }

            while (Date.now() < deadline) {
              const response = await github.rest.repos.getCombinedStatusForRef({ owner, repo, ref });
              const statuses = response.data.statuses.filter((status) =>
                matchContext(status.context)
              );

              if (statuses.length === 0) {
                core.info(`Waiting for Vercel status "${targetName}"...`);
              } else {
                const latestStatuses = latestPerContext(statuses);
                const summary = latestStatuses
                  .map((s) => `${s.context} (${s.state})`)
                  .join(', ');
                core.info(`Latest Vercel statuses: ${summary}`);
                if (latestStatuses.some((s) => ['failure', 'error'].includes(s.state))) {
                  const failed = latestStatuses.find((s) => ['failure', 'error'].includes(s.state));
                  throw new Error(`Vercel status ${failed.context} concluded with ${failed.state}`);
                }
                if (latestStatuses.every((s) => s.state === 'success')) {
                  core.info('Vercel statuses succeeded.');
                  return;
                }
              }

              await new Promise((resolve) => setTimeout(resolve, pollSeconds * 1000));
            }

            throw new Error(`Timed out waiting for Vercel check containing "${targetName}".`);

  mcp-confirmation:
    runs-on: ubuntu-latest
    needs: [changes, preflight, ci-gate]
    if: needs.changes.outputs.functions == 'true'
    timeout-minutes: 240
    environment:
      name: insforge-mcp
    steps:
      - name: MCP deploy confirmation
        run: |
          echo "Functions changed. Deploy via Insforge MCP, then approve this environment to continue."

  mcp-skip:
    runs-on: ubuntu-latest
    needs: [changes, preflight, ci-gate]
    if: needs.changes.outputs.functions == 'false'
    steps:
      - name: Skip MCP gate (no function changes)
        run: echo "No function changes detected; MCP gate satisfied."
